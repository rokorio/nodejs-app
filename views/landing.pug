extends layout

block content
  h1= title
  p Welcome to #{title}
  form(action='/',method='POST')
      input(name="lead_email" ,type="text", placeholder='enter your email address')
      button(type="submit") Submit
